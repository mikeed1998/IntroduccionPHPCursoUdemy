<?php include 'includes/header.php';

// Boolean
$logueado = false;
var_dump($logueado);

// Enteros
$numero = 200;
var_dump($numero);

// Floats
$float = 200.5;
var_dump($float);

// Strings
$nombre = "Michael";
var_dump($nombre);

// Array
$array = [];
var_dump($array);

include 'includes/footer.php';